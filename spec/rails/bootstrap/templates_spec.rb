require 'spec_helper'

describe Rails::Bootstrap::Templates do
  it 'has a version number' do
    expect(Rails::Bootstrap::Templates::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
